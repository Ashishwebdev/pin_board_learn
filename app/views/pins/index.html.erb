<html>

  <% @pin.each do |pin| %>

    <%= link_to (image_tag "#{pin.image}") %>
      <%= pin.title %>
    <%= link_to "Show", pin %>
    <% if user_signed_in? %>
        <%if pin.user == current_user %>
    <td> <%= link_to "Edit", edit_pin_path(pin) %> </td>
            <% end %>
  <% end %>


  <% end %>
  </br>

  </html>